[package]
name = "twistedflow-plugin"
version = "0.1.0"
edition = "2021"
description = "Guest SDK for writing TwistedFlow WASM plugin nodes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
